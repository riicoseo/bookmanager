spring:
  h2:
    console:
      enabled: true
  jpa:
    open-in-view: false
    generate-ddl: true
    ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:mariadb://localhost:3306/bookmanager
    username: root
    password: riicoseo